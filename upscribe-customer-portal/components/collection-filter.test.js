import CollectionFilter from './collection-filter.vue'

describe('@components/CollectionFilter', () => {
  it('exports a valid component', () => {
    expect(CollectionFilter).toBeAComponent()
  })
})
