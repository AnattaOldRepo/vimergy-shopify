import DeliveryFrequencyItem from './delivery-frequency-item.vue'

describe('@components/DeliveryFrequencyItems', () => {
  it('exports a valid component', () => {
    expect(DeliveryFrequencyItem).toBeAComponent()
  })
})
