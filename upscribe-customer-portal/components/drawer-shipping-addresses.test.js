import DrawerShippingAddresses from './drawer-shipping-addresses.vue'

describe('@components/DrawerShippingAddresses', () => {
  it('exports a valid component', () => {
    expect(DrawerShippingAddresses).toBeAComponent()
  })
})
