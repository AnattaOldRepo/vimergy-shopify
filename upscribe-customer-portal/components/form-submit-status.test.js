import FormSubmitStatus from './form-submit-status.vue'

describe('@components/FormSubmitStatus', () => {
  it('exports a valid component', () => {
    expect(FormSubmitStatus).toBeAComponent()
  })
})
