import PortalBlockOption from './portal-block-option.vue'

describe('@components/PortalBlockOption', () => {
  it('exports a valid component', () => {
    expect(PortalBlockOption).toBeAComponent()
  })
})
