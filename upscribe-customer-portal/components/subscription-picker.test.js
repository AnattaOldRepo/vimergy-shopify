import SubscriptionPicker from './subscription-picker.vue'

describe('@components/SubscriptionPicker', () => {
  it('exports a valid component', () => {
    expect(SubscriptionPicker).toBeAComponent()
  })
})
