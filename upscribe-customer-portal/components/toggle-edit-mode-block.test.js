import ToggleEditModeBlock from './toggle-edit-mode-block.vue'

describe('@components/ToggleEditModeBlock', () => {
  it('exports a valid component', () => {
    expect(ToggleEditModeBlock).toBeAComponent()
  })
})
