import OneTimeProductAddPage from './one-time-product-add.vue'

describe('@views/one-time-product-add', () => {
  it('is a valid view', () => {
    expect(OneTimeProductAddPage).toBeAPageComponent()
  })
})
